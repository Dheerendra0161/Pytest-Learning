"""
The __init__.py file acts as a signal to Python that this directory should
be treated as a package, not just a folder. This allows you to import modules
from that directory using the package syntax
"""
